<ul>
<% @songs.each do |song| %>
  <li>
    <%= link_to song.title, song_path(song) %> 
    <%= link_to display_artist(song)[0], display_artist(song)[1] %>
  </li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
